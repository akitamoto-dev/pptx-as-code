---
name: pptx-lint
description: "出来上がったスライドを検査する。利用者が明示的に求めたときだけ使う。全ページの PNG を読んで規範と照合するため、時間とコストがかかる。作成したページのビルド済み PNG を読み、はみ出し・重なり・文字化け・配色・文章規範の違反をページ番号付きで列挙する。原稿を渡せば、文言・コード・リンク先が原稿と一致しているかも照合する。体裁・規範・転記の一致だけを見て、内容そのものの妥当性（事実誤認や論理）は判断しない。修正もしない。「スライド検証」「全ページ確認」「レイアウト崩れ」「体裁チェック」でトリガー。"
context: fork
background: false
---

# pptx-lint

対象の資料を次の手順で検証し、指摘だけを返す。会話の履歴は前提にしない。修正はしない。

**利用者が明示的に求めたときだけ呼ばれる。** 作成スキル（content-to-pptx、image-to-pptx）は既定では呼ばない。全ページの PNG を読んで規範と照合するため、1 ページでも数分かかり、推論のコストも大きい。時間とコストをかけて完成度を高めたい場合に使う。

## 手順

1. **対象を決める**。呼び出し文にディレクトリの指定があればそれ。無ければ、カレントディレクトリから `deck.json` と `deck-src/` を持つディレクトリを探す（カレント自身、次に直下の子）。見つからなければ「対象の資料が見つからない」と 1 行で返して終わる
2. **PNG を用意する**。出力名は `deck.json` の `name`（呼び出し文に `--name` があればそれ）。`preview/<name>-NNN.png` が無い、または `<name>.pptx` より古ければ `node build.js --name <name>` を実行する。ビルドが規格違反で止まったら、その出力を指摘として返す
3. **範囲を決める**。呼び出し文に `--pages 3-5` のような指定があればそのページだけ。無ければ資料の全ページ。**作成・変更したのが一部であれば呼び出し側が範囲を添える約束**なので、指定が無ければ全体が対象で正しい
4. **原稿があれば読む**。呼び出し文に原稿のパスがあればそれを読み、照合の対象にする。無ければ照合は行わない
5. **1 枚ずつ読む**。PNG を Read し、下の観点で検査する。PDF や pptx は読まない。**PNG は 1 ページにつき 1 回だけ読む。** 拡大した切り出しを作って読み直すのは、はみ出し・重なり・文字化けが疑われる箇所に限る。読み込みの回数がそのまま時間とコストになる
6. **返答の形式で返す**。所感・称賛・修正案の本文は書かない

## 検査の観点

文章と構成の規範は、使っている書式スキルが持つ。所定の青基調なら [../blue-format/writing-rules.md](../blue-format/writing-rules.md) が正本。迷ったらそれに従う。
書式スキルを使っていない場合は、レイアウト・描画・色の 3 種別だけを見る。

| 種別 | 見るもの |
|---|---|
| レイアウト | 文字のはみ出し、意図しない折り返し、要素の重なり（後から描いた枠がテキストを隠している）、出典行の折り返し、本文領域の下端からの逸脱、余白が大きすぎる（情報不足） |
| 描画 | 文字化け（豆腐）、アイコンの欠落、細すぎて消えた線、文字が小さすぎて読めない |
| 色 | 青系・白・グレー以外の色の混入、赤が 1 枚に複数箇所、並列する項目の 1 つだけの着色、補足や結論を囲む色付きの枠、表のデータ行の塗り |
| 階層 | タイトルが体言止めでない、キーメッセージがですます調の完全文でない（1 文なら句点なし、2 文なら 1 文目だけ句点）、本文がである調でない、＜見出し＞が短い名詞句でない・個数入り・直下に一文が無い |
| 構成 | 下部のまとめ帯やキーメッセージの再掲、章番号・他スライド・資料自体への言及、図の下の説明文、本文での図の名指し、数の言い切り、口語・比喩・造語、一直線のフロー図、番号の対応先が無い、対比が左右非対称 |
| 照合 | （原稿を渡されたときだけ）原稿にある文言・数値・コードの欠落、誤字・全角半角・単位の食い違い、リンクの文字列と原稿の出典の不一致。**書かれている内容が正しいかは判断しない。**原稿との差だけを見る |

## 返答の形式

1 行目は集計。2 行目以降は指摘を 1 行ずつ。指摘が無ければ「指摘なし」の 1 行だけ。

```
9 ページ中 3 ページに 4 件
p4: 出典行が 2 行に折り返し、本文と重なる（レイアウト）
p4: ＜守る原則＞の直下に説明の一文が無い（階層）
p7: 戻りの矢印のラベルが破線と重なる（レイアウト）
p9: 本文に「図のとおり」と図の名指しがある（構成）
p9: 原稿にある「既定で 30 日間保存される」の「既定で」が欠けている（照合）
```

ページ番号は PNG のファイル名の番号。同じページに複数あれば行を分ける。
